# Minimal Security Dependencies for MetinAnaliz
# Install with: pip install -r requirements-security-minimal.txt
# Note: fastapi, pydantic, and reportlab are already installed

# Environment variables management
python-dotenv==1.0.0

# Rate limiting (optional - can use the built-in middleware first)
# slowapi==0.1.9
# redis==5.0.1

# For enhanced logging (optional)
# python-json-logger==2.0.7
